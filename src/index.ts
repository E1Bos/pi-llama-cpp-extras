import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

// Placeholder entry. Later tickets fill this in: the progress display
// (prefill bar, thinking counter) and its provider wiring.
export default (_pi: ExtensionAPI): void => {};
